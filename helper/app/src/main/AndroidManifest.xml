<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to open a ServerSocket at all, even one bound to loopback. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:hasFragileUserData="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">

        <!-- Exported because the system binds accessibility services, and gated
             by BIND_ACCESSIBILITY_SERVICE so only the system can. -->
        <service
            android:name=".PilotAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Jev Pilot UI automation bridge" />
        </service>

        <!-- Session token drop. Only a sender holding WRITE_SECURE_SETTINGS -
             the adb shell user, or the system - can deliver this, so no
             ordinary app on the device can set a token of its own and then
             talk to the loopback server. -->
        <receiver
            android:name=".TokenReceiver"
            android:exported="true"
            android:permission="android.permission.WRITE_SECURE_SETTINGS">
            <intent-filter>
                <action android:name="dev.jevpilot.helper.SET_TOKEN" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
